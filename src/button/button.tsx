import React, { memo, FC } from 'react';
import styled from 'styled-components';

export const Button: FC<any> = memo(() => {
  return <Container></Container>;
});

const Container = styled.button``;
